[!]
$ /usr/bin/env python3
$ -*- coding: utf-8 -*-
>>> from core.compatible import check_dependencies
>>> enter: point*OWASP_NETTACKER_FRAME \work-flow
>>> __check_external_modules create --check requirements.txt for load(ENGINE):
    if __name__ == "__main__":
        check_dependencies(ENGINE)
            for dependencies
                `
                if dependencies and OS requirements are match then load the program
                    from core.parse import load(ENGINE)
`
>>> load(ENGINE)&&parseARGV
>>> sys.exit(main(ENGINE))
`
